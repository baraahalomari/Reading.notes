# React lifecycle

## What does componentDidMount do?
 Render

## What is the very first thing to happen in the lifecycle of React?
Constructor

## Put the following things in the order that they happen: componentDidMount, render, constructor, componentWillUnmount, React Updates 
* constructor
* render
* React Updates
* componentDidMount
* componentWillUnmount

## What does componentDidMount do?
it's sub-components have rendered properly.

# React State Vs Props

## What types of things can you pass in the props?
The props can be anything from integers over objects to arrays.

## What is the big difference between props and state?
Props: are like arguments to functions, props pass into a component which means It is handled outside the component and must be updated outside the component State: something inside a component, is handled inside the component and must be updated inside the component


## When do we re-render our application?
When the user makes a change.

## What are some examples of things that we could store in the state?
Numbers, strings, Boolean, Array, Opject..etc

